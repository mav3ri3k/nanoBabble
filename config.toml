train_data_path = "train.parquet"
data_column = "token_id"
checkpoint_dir = "checkpoints"
metrics_dir = "metrics"
experiments_db = "experiments.db"
experiment_id = "default"

seed = 42
batch_size = 16
seq_len = 32
epochs = 1
learning_rate = 0.0003
checkpoint_every_steps = 20
log_every_steps = 5
resume = false

vocab_size = 8000
dim = 128
num_heads = 4
num_layers = 4
ffn_dim = 256

attn = "mha"
attn_local = "swa"
attn_global_every = 1
swa_window = 128
mla_latent_dim = 128
